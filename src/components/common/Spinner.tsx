const Spinner = () => (
  <div className="m-12 flex justify-center items-center w-16 h-16 rounded-full bg-gradient-to-b from-brand-600 via-transparent to-transparent">
    <div className="relative w-2 h-2 bg-brand-600 rounded-full animate-spin" />
  </div>
);

export default Spinner;
